<?php

namespace App\Controller;

use Symfony\Component\HttpFoundation\Response;

class {{ className }} extends Response
{
    public function __construct(
{%~ if content.schema is defined and content.schema.type is same as ('object') %}
        {{ toObjectSchemaClassName(spec, content.schema, className ~ 'Payload') }}{{ className ~ 'Payload' }} $payload,
{%~ endif %}
    ) {
        parent::__construct(
            '',
            {{ code }},
            [],
        );
    }
}