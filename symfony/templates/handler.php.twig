{% autoescape false %}
<?php

namespace {{ file.namespace }};

interface {{ file.className }}
{
{%~ for action in file.actions %}
    {%~ for method in action.methods %}
    public function {{ method.name }}(
        {%~ for param in method.parameters %}
        {{ param.type.methodParameterType }} ${{ param.variableName }},
        {%~ endfor %}
        {%~ if method.requestBodyPayloadType is not null %}
        {{ method.requestBodyPayloadType.methodParameterType }} $requestBodyPayload,
        {%~ endif %}
    ):
        {%~ for response in method.responses %}
        {{ response }}{%~ if not loop.last %} |
        {%~ else %};
        {%~ endif %}
        {%~ endfor %}
        {%~ if not loop.last %}

        {%~ endif %}
    {%~ endfor %}
    {%~ if not loop.last %}

    {%~ endif %}
{%~ endfor %}
}
{% endautoescape %}