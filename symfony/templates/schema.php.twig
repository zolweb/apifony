<?php

namespace App\Controller;

use Symfony\Component\Validator\Constraints as Assert;

class {{ className }}
{
    /**
{%~ for propertyName, property in schema.properties %}
    {%~ if property.type is defined and property.type is same as ('array') %}
     * @param {{ toParamArrayAnnotation(spec, className, schema, property, propertyName)|raw }}
    {%~ endif %}
{%~ endfor %}
    */
    public function __construct(
{%~ for propertyName, property in schema.properties %}
    {%~ for constraint in getPropertyConstraints(schema, property, propertyName) %}
        #[{{ constraint|raw }}]
    {%~ endfor %}
        public readonly {{ propertyToMethodParam(spec, className, schema, property, propertyName) }},
{%~ endfor %}
    ) {
    }
}