<?php

namespace App\Controller;

use Symfony\Component\Validator\Constraints as Assert;

class {{ schema.className }}
{
    /**
{%~ for property in schema.arrayProperties %}
     * {{ toParamArrayAnnotation(className, schema, property, propertyName)|raw }}
{%~ endfor %}
    */
    public function __construct(
{%~ for propertyName, property in schema.properties %}
    {%~ for constraint in getPropertyConstraints(schema, property, propertyName) %}
        #[{{ constraint|raw }}]
    {%~ endfor %}
        public readonly {{ propertyToMethodParam(className, schema, property, propertyName) }},
{%~ endfor %}
    ) {
    }
}