{% autoescape false %}
<?php

namespace App\Controller;

use Symfony\Component\Validator\Constraints as Assert;

class {{ schema.className }}
{
{%~ if schema.type.arrayProperties %}
    /**
    {%~ for property in schema.type.arrayProperties %}
     * {{ property.phpDocParameterAnnotation }}
    {%~ endfor %}
     */
{%~ endif %}
    public function __construct(
{%~ for property in schema.properties %}
    {%~ for constraint in property.constraints %}
        {{~ constraint.annotation(2) }}
    {%~ endfor %}
        public readonly {{ property.methodParameter }},
    {%~ if not loop.last %}

    {%~ endif %}
{%~ endfor %}
    ) {
    }
}
{% endautoescape %}