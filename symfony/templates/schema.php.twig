<?php

namespace App\Controller;

{% set schema = spec.components.schemas[name] %}
class {{ name }}Schema
{
    /**
    {%~ for name, property in schema.properties %}
    {%~ if property.type is defined and property.type is same as ('array') %}
    {%~ if property.items.type is defined %}
     * @param array<{{ property.items.type|toPhpType }}> ${{ name }}
    {%~ else %}
    {{ genSchema(spec, property.items['$ref']) }} * @param array<{{ property.items['$ref']|toSchemaClassName }}> ${{ name }},
    {%~ endif %}
    {%~ endif %}
    {%~ endfor %}
    */
    public function __construct(
        {%~ for name, property in schema.properties %}
        {%~ if property.type is defined %}
        public readonly {{ property.type|toPhpType }} ${{ name }},
        {%~ else %}
        public readonly {{ genSchema(spec, property['$ref']) }}{{ property['$ref']|toSchemaClassName }} ${{ name }},
        {%~ endif %}
        {%~ endfor %}
    ) {
    }
}