{% autoescape false %}
<?php

namespace App\Controller;

use Symfony\Component\Validator\Constraints as Assert;

class {{ schema.className }}
{
{%~ if schema.type.arrayProperties %}
    /**
    {%~ for name, property in schema.type.arrayProperties %}
     * {{ property.phpDocParameterAnnotation(name) }}
    {%~ endfor %}
     */
{%~ endif %}
    public function __construct(
{%~ for name, property in schema.properties %}
    {%~ for constraint in property.constraints %}
        {{~ constraint.annotation(2) }}
    {%~ endfor %}
        public readonly {{ property.methodParameter(name) }},
    {%~ if not loop.last %}

    {%~ endif %}
{%~ endfor %}
    ) {
    }
}
{% endautoescape %}