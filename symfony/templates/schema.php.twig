<?php

namespace App\Controller;

use Symfony\Component\Validator\Constraints as Assert;

class {{ className }}
{
    /**
{%~ for propertyName, property in schema.properties %}
    {%~ if property.type is defined and property.type is same as ('array') %}
     * @param {{ toParamArrayAnnotation(spec, className, schema, property, propertyName ) }}
    {%~ endif %}
{%~ endfor %}
    */
    public function __construct(
{%~ for propertyName, property in schema.properties %}
    {%~ for constraint in getParamConstraints(param) %}
        #[{{ constraint|raw }}]
    {%~ endfor %}
        public readonly {{ propertyToMethodParam(schema, property, propertyName) }},
{%~ endfor %}
    ) {
    }
}