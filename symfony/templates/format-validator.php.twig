<?php

namespace {{ validator.namespace }};

use Symfony\Component\Validator\Constraint;
use Symfony\Component\Validator\ConstraintValidator;

class {{ validator.className }} extends ConstraintValidator
{
    private {{ validator.definitionInterfaceName }} $formatDefinition;

    public function setFormatDefinition({{ validator.definitionInterfaceName }} $formatDefinition): void
    {
        $this->formatDefinition = $formatDefinition;
    }

    public function validate(mixed $value, Constraint $constraint): void
    {
        foreach ($this->formatDefinition->validate($value) as $violation) {
            $this->context->buildViolation($violation)->addViolation();
        }
    }
}